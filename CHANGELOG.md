# 3.2.0

本次更新加入从配置到首次单次运行的完整自动定点乱数引导，并完善伊机控脚本的查看、录制、调试与继续运行体验。

### 首次乱数引导

- 首次启动可选择「我是小白」进入引导，或直接进入工作区；已有用户可从顶部「开始引导」进入，也可在帮助菜单重新打开乱数方式选择。引导进度会保存，支持收起、继续和重新开始。
- 引导覆盖目标精灵、搜索与 delay 策略、视频源与伊机控连接、Seed 捕捉、脚本配置、OCR 核对和首次单次运行。实际控件高亮提示，跨页时等待用户点击对应标签，完成连接或保存后衔接下一步。
- 新增眼睛 ROI 与模板框选、键盘操作录制、OCR 框选教学演示；结合独立预览进行实际操作，提示御三家选择、同步队伍和推荐测种地点等准备条件。
- 脚本教学支持测种、1000 帧试跑、校正、撞帧和反查的逐项验证；运行后确认游戏结果，失败时可修改脚本并按前置步骤重试，也可用虚拟手柄复原现场后继续。
- OCR 引导先执行「测试全部」，再选择有问题的项目逐项修复；重新框选、识别并人工确认后复测。保留部分修复进度，并提示在笔记页与能力页之间切换。
- 首次单次运行结束后，可查看本轮唯一有效的反查 delay 样本及对应日志。停止、失败、多候选和疑似出闪会分别提示下一步；历史样本不会被当成本次成功结果。

### 伊机控脚本体验

- 新增当前脚本与可折叠的 lib 脚本库，可预览内置脚本并双击打开编辑；切换时保留未保存草稿确认，执行跟随不会覆盖当前编辑内容。
- 运行时高亮实际执行行，显示动作、等待剩余时间、循环轮次和跨库调用位置；支持自动跟随与手动定位，停止或完成后保留最后执行位置。
- 手动脚本新增「暂停」和「继续运行」。WAIT 可立即暂停并保留剩余时间，按键与摇杆在当前动作完成后暂停；暂停期间可修改尚未执行的代码，继续时直接编译当前草稿并保留变量、循环和调用进度。无效修改会提示原因并保持暂停；已执行语句与已加载的库集合不能随意修改。此功能不用于自动乱数流程，暂停脚本也不会暂停游戏或 RNG。
- 录制时按键与等待命令实时追加到编辑器末尾并跟随显示，支持暂停和继续录制；停止时不再重复插入已显示的内容。
- 纯按键、等待和循环脚本无需先连接视频源；只有实际执行搜图或 OCR 语句时才读取画面，并在视频源不可用时提示具体原因。包含手柄操作的脚本仍需连接伊机控。

### 界面与内置脚本

- 调整眼睛区域操作按钮顺序，统一绿色焦点边框，将目标筛选的身高、体重字段中文化；改善同步与自动反查字段对齐、窄窗口提示换行及引导弹窗的滚动和定位。
- 校正策略窗口明确默认 90 万帧、最多 100 万帧，并补充过场预留帧数的作用说明；修复独立预览布局变化后的画面缩放与框选坐标更新。
- 调整御三家撞帧脚本的精灵选择方式与注释；两份 BDSP 过帧脚本的默认目标帧数改为 1000，便于试跑；补充测种快捷道具及游走反查大师球位置的调整说明。

### 文档与开发

- 重整项目 README，补充界面截图、设备要求、快速开始与常见问题，并拆分详细使用指南和开发指南。
- 开发入口新增 Mock 视频源、Mock 串口，以及手动 Seed 捕捉与校正的成功/失败选择，方便在无硬件时验证界面和引导；模拟结果仅用于开发测试。

# 3.1.1

- 修复软件异常退出后，残留的视频源记录可能持续阻止采集卡重新连接的问题；共享画面内存失效时仍可发送退出请求，避免将被其他程序复用的旧进程编号误判为采集卡占用。
- 改善 Media Foundation 对部分采集卡的连接兼容性，调整视频格式设置并默认使用软件视频转换，减少驱动硬件转换初始化导致的连接卡顿。
- 调整 DirectShow 的帧率与视频格式设置顺序，避免设置帧率后丢失 MJPEG 压缩格式，改善部分采集卡在本软件中帧率偏低的问题。
- 将视频源进程启动、设备打开和首帧等待分开计时，避免较慢的初始化耗尽首帧等待时间；连接失败时明确提示实际超时阶段或后端不可用原因。
- 视频源连接成功和失败时均记录采集诊断，包括实际分辨率、驱动报告的帧率与格式，以及未被设备接受的设置；采集读取卡住时也能处理退出请求，便于释放视频源并重试。

# 3.1.0

- 自动 TID 数据表新增“累计用时”和“预计到达时间”，沿用 Project_Xs 小卡比兽眨眼时间模型计算。测种后显示固定的本地日期时间（如 `2026.08.11 17:40:40`），随实际帧进度标记“已过”或“当前帧”；悬停仍可查看原预计时间。
- 自动 TID 运行卡显示“距取名启动 · 预计”倒计时，以及触发帧和本轮 delay；倒计时刷新不重建数据表，停止、重新测种或开始取名时结束该次倒计时。
- TID 表格用带结果数量的“全部 TID / 仅目标 TID”分段切换和独立“定位目标”按钮替代表格设置，支持查看所有匹配目标。切换控件采用浅灰容器、白底绿色选中项，定位按钮附准星图标；运行中仍按本轮启动时的目标筛选。
- TID 的“复制全部”和“导出全部 CSV”保留完整生成结果、累计用时和固定日期，不受仅目标筛选影响。手动输入 Seed 时可查看相对用时，实际测种后才附加日期基准。
- 自动定点校正在预计帧数附近未命中时，会复用本次眨眼观测扩大搜索范围；`1 PK NPC` 校正按窗口覆盖更大范围，减少长距离过帧偏离预计窗口造成的校正失败。新增 25 万、50 万、90 万帧的原生计算与 Project_Xs 原版对照测试，返回 Seed 和帧数一致。
- 优化六页工作区、连接/关于/OCR 弹窗与结果表，统一冷白、翡翠绿及随包 MiSans 字体；自动流程突出运行状态，脚本和详情可折叠，Seed 页默认采用约 3:7 分栏，小窗口支持上下排列与独立滚动。
- 增加开始前准备检查、统一“日志中心”入口，完善定点表格排序、选中行复制、列控制和筛选方案；切换能力值显示时避免重建整表，减少卡顿。
- 自动定点与自动 TID 分别保存任务参数和脚本选择，并显示各自未保存状态；输入框和下拉框不再因页面滚动意外改值。

同时包含 v3.0.4 的以下更新：

- 自动定点的 delay 配置和反查样本按当前精灵独立保存并跨启动恢复；设置窗口支持推荐 delay、最近与完整样本记录、分页、按轮划除/恢复和清空当前精灵样本，并明确区分本轮冻结值与下轮预计值。
- 完善确认版紧凑工作区：统一视频源与伊机控状态入口，补全自动定点、自动 TID、Seed 捕捉、定点数据和日志区的操作入口，调整目标选择、结果表、脚本操作和常用控件的布局与视觉细节，改善窄屏和高 DPI 下的可读性。
- 修复共享 Capture Broker 在长时间运行时偶发读取到写入竞争产生的空快照，导致预览误判视频源断联的问题；真实无帧、超时或采集故障仍会正常停止并提示。

# 3.0.4

- 自动定点的 delay 配置和反查样本按当前精灵独立保存并跨启动恢复；设置窗口支持推荐 delay、最近与完整样本记录、分页、按轮划除/恢复和清空当前精灵样本，并明确区分本轮冻结值与下轮预计值。
- 完善确认版紧凑工作区：统一视频源与伊机控状态入口，补全自动定点、自动 TID、Seed 捕捉、定点数据和日志区的操作入口，调整目标选择、结果表、脚本操作和常用控件的布局与视觉细节，改善窄屏和高 DPI 下的可读性。
- 修复共享 Capture Broker 在长时间运行时偶发读取到写入竞争产生的空快照，导致预览误判视频源断联的问题；真实无帧、超时或采集故障仍会正常停止并提示。

# 3.0.3

- 自动定点新增“校正策略设置”：可配置校正帧数上限、普通校正最大尝试次数、连续失败后的处理、重测 Seed 最大尝试次数和过场预留帧数；普通校正失败后可直接进入下一轮，也可先完整重测 Seed，成功后清除旧目标并在本轮重新搜索。
- 过场脚本运行后继续采用独立的安全规则：过场校正固定最多 2 次，失败或后续过帧超过校正上限时直接进入下一轮，不会在当前位置重测 Seed；主动停止也不会触发后续重试或测种。
- 自动定点 delay 新增固定 delay、上次实际 delay、众数、中位数、滚动平均值、指数平滑、截尾平均和密集区间 8 种策略，可配置基准 delay、统计窗口、多候选处理、平滑权重和密集区间跨度；默认保持固定策略，并兼容已有 `fixed_delay` 设置。
- 自动反查结果会按轮保存为 delay 样本，多候选轮次可选择忽略或按轮加权；界面会显示本次使用、下轮预计和有效样本，策略与样本跨启动保留，当前已锁定目标的 delay 不会在轮内变化。

# 3.0.2

- 优化自动反查的个性 OCR：固定识别区域未匹配到标准个性时会改用全图重试；仍无结果时提示“个性匹配失败”并跳过个性过滤，继续按性格和能力值返回候选。
- 将“历史记录”整合为顶层“日志区”：轮次记录采用主从视图，详细日志支持来源、级别、关键词和轮次筛选；业务页改为最近消息与统一日志入口，关闭磁盘保存后仍保留当前会话实时日志。
- 修正自动定点和伊机控最近消息区的按钮对齐与长文本重叠，优化日志区标题、空状态及详细日志底部操作栏的留白；伊机控不再显示测试用 `mock` 串口，并统一计时器与运行按钮的尺寸和配色。
- Windows 键盘虚拟手柄改用系统级按键捕获，并新增复用原版 EasyCon `100x100` VPad 素材和绘制坐标、读取真实手柄报告的状态悬浮窗；脚本启动、断线和退出时会先停用键盘输入并释放残留状态，避免键盘操作干扰脚本。
- 虚拟手柄支持按 `Esc` 立即切换启用/待机、按 `Ctrl+Esc` 完全关闭并隐藏；待机状态只保留快捷键监听，其他映射键正常透传，按住的手柄输入会先安全释放。
- 按键映射窗口支持右键删除单个映射，未绑定状态会立即显示并随配置保存。
- 伊机控连接入口移至全局顶部并采用与视频源一致的状态展示和独立设置窗口；键盘控制和操作录制迁入页面左侧空白区，以“关闭 / 待机 / 控制”三态直接展示真实状态，同时释放脚本编辑区高度，并移除含义不清的“状态窗口”按钮和单项输入下拉框。
- 新增“御三家测种”“御三家撞帧”和“御三家反查”三个内置脚本，可直接从伊机控及自动流程的脚本列表中选择。

# 3.0.1

- 新增应用级界面缩放，可自动按所有已连接显示器中最受限的工作区选择倍率，也可从帮助菜单以 5% 步进手动调整。
- 小屏适配改为保持原有页面结构并整体等比例缩小，取消整页滚动包装和 Seed 页自动上下重排。
- 修正低于 100% 界面倍率时的视频预览像素比处理，保持主预览、独立预览和 ROI 坐标一致。
- 改进自动反查的宝可梦信息 OCR：支持更多中文、生命值和能力值版式，并在识别不完整时自动扩大能力值区域重试，减少误判和反查失败。
- 自动定点搜索会排除活帧计数器无法到达的目标帧，避免因 NPC 步长造成的不可达候选误触发或反复重测种。

# 3.0.0

- 重做视频源连接与预览，采集卡画面可同时用于主预览、独立预览、Seed/TID 捕捉、OCR 和伊机控；新增 OBS 投影窗口捕捉，并改进视频源及眼睛区域配置错误提示
- Windows 绿色包内置原生伊机控运行环境，无需另行安装 EasyCon、EasyConBridge 或 `ezcon`；页面脚本、自动流程和虚拟手柄共用同一串口连接
- 脚本统一放在 exe 同级的 `script` 目录，旧选择和用户自定义脚本会保留；新增神奥图鉴和优化版 BDSP 过帧脚本，并补充、优化多份定点和游走脚本
- 新增应用内增量更新，默认在启动时静默检查且可在帮助菜单关闭；更新会保留用户脚本和配置，失败时自动恢复旧版
- 自动定点新增可选的“逃跑续搜”，并优化候选结果、反查历史、脚本选择及主动停止流程
- 优化 OCR 默认区域、判闪识别和任务管理，减少首次识别等待与并发误判，并兼容繁体版本
- 选择草苗龟、小火焰猴或波加曼时，判闪计时会改用“去吧/上吧”到战斗按钮出现的时间差；其他目标的判闪方式保持不变
- 新增默认开启的七天运行日志，每天汇总为一个文件，可在“帮助 -> 运行日志”中关闭、查看或打开日志目录
- 优化小屏和高 DPI 下的界面布局、独立预览框选、参数说明、更新日志及用户可见的状态和错误提示

# 2.1.7

- 优化 OCR 内置模型预热：安装包强制校验并使用随包模型，预热期间禁用同步识别与测试，减少首次识别等待和并发异常

# 2.1.6

- 自动定点和自动 TID 在后续轮次运行测种脚本前检测“缩放/縮放”“锁定/鎖定”“重新调整/重新調整”等放大提示，必要时通过双 HOME 自动退出放大模式，并完善停止竞态保护
- 启动时预热共享 OCR 资源，完善预热线程的关闭与复用生命周期，减少流程首次 OCR 的等待
- 20、40、64 次眨眼捕捉每累计 10 次会通过伊机控短按 L 肩键保持 Switch 亮屏，覆盖手动 Seed/TID、自动定点和自动 TID 捕捉
- 完善亮屏保活的 CLI/Bridge 非阻塞、超时和故障恢复；伊机控异常不会冻结界面或中断捕捉，失效 Bridge 可直接重新连接

# 2.1.5

- 普通 reidentify 与 `Reidentify 1 PK NPC` noisy reidentify 优先使用 C++ 原生搜索；结果与 Project_Xs 原版一致，原生不可用、输入不支持或无匹配时继续回退到 Python 实现
- 自动流程按预期 advances 缩小重新识别范围并记录搜索范围、计算后端和耗时；手动 noisy reidentify 保留当前活帧快照，避免停止活帧后从 0 帧重新搜索
- 原生重新识别显著减少 Python 列表、切片和候选分配，普通 100 万帧与 noisy 10 万帧搜索在本机基准中均获得约百倍加速
- 修正 Python 回退与 C++ 原生定点生成器的 RNGList 推进及强制闪 PID 计算，使两条路径继续与 PokeFinder 样例一致

# 2.1.4

- 修复关于项目页微信/支付宝赞赏按钮在二维码缺失或读取失败时看起来无响应的问题
- 自动定点“预留帧数”默认值调整为 500000，已有历史设置时继续恢复用户记录

# 2.1.3

- 修复自动定点目标添加未同步存档版本的问题，明亮珍珠可正确选择急冻鸟、闪电鸟、火焰鸟等版本限定目标
- 补全三鸟反查组，选择急冻鸟、闪电鸟、火焰鸟任一目标时会按三鸟整组反查
- 自动定点闪光阈值默认调整为 4.0 秒，已有历史设置时继续恢复用户记录
- 修复错误眼睛区域可能稳定算出同一假 seed 后反复无候选、无限循环的问题
- 修复主窗口存档信息不会记忆的问题，重开软件后会恢复存档名称、TID、SID、版本和护符/图鉴勾选状态

# 2.1.2

- 优化自动TID目标池布局

# 2.1.1

- 优化自动乱数中的输出

# 2.1.0

- 修复ocr识别bug：原本只识别`去吧`作为判断，现在额外增加`上吧`作为判断

# 2.0.2

- 增加ocr判闪机制：如果识别到 `出现了!`会多等30s `去吧`才会判定为超时
- 增加`出现了!`和`去吧`的时间输出

# 2.0.1

- 增加单片机连接失败提示
- 修复主动停止却有失败弹窗

# 2.0.0

- 反查失败输出能力值
- 修复过帧脚本的切换精灵失败问题
- 增加自动TID功能
- 增加过场脚本和预留帧数（为在精灵面前测帧的需求增加）
- 增加ocr预热以及ocr范围框选
- 修复手柄按键映射无法修改问题
- 增加自行校验闪光按钮

# 1.0.1

- 增加框选眼睛时的确认框
- 修复帧推进异常问题
- 优化撞闪脚本的触发方式

# 1.0.0

- 支持对定点宝可梦进行自动化乱数，目前阿尔宙斯和谢米内置脚本
- 支持自动反查功能（检查具体delay）
- 支持同步功能（队首同步可更改宝可梦体型从而高效寻找目标）
- 支持伊机控CLI模式和常驻连接模式（常驻连接尚有bug）
- 支持多目标筛选
- 增加三狗、三柱、水都的多反查机制
