# 3.0.2

- 优化自动反查的个性 OCR：固定识别区域未匹配到标准个性时会改用全图重试；仍无结果时提示“个性匹配失败”并跳过个性过滤，继续按性格和能力值返回候选。
- 将“历史记录”整合为顶层“日志区”：轮次记录采用主从视图，详细日志支持来源、级别、关键词和轮次筛选；业务页改为最近消息与统一日志入口，关闭磁盘保存后仍保留当前会话实时日志。
- 修正自动定点和伊机控最近消息区的按钮对齐与长文本重叠，优化日志区标题、空状态及详细日志底部操作栏的留白；伊机控不再显示测试用 `mock` 串口，并统一计时器与运行按钮的尺寸和配色。
- Windows 键盘虚拟手柄改用系统级按键捕获，并新增复用原版 EasyCon `100x100` VPad 素材和绘制坐标、读取真实手柄报告的状态悬浮窗；脚本启动、断线和退出时会先停用键盘输入并释放残留状态，避免键盘操作干扰脚本。
- 虚拟手柄支持按 `Esc` 立即切换启用/待机、按 `Ctrl+Esc` 完全关闭并隐藏；待机状态只保留快捷键监听，其他映射键正常透传，按住的手柄输入会先安全释放。
- 按键映射窗口支持右键删除单个映射，未绑定状态会立即显示并随配置保存。

# 3.0.1

- 新增应用级界面缩放，可自动按所有已连接显示器中最受限的工作区选择倍率，也可从帮助菜单以 5% 步进手动调整。
- 小屏适配改为保持原有页面结构并整体等比例缩小，取消整页滚动包装和 Seed 页自动上下重排。
- 修正低于 100% 界面倍率时的视频预览像素比处理，保持主预览、独立预览和 ROI 坐标一致。
- 改进自动反查的宝可梦信息 OCR：支持更多中文、生命值和能力值版式，并在识别不完整时自动扩大能力值区域重试，减少误判和反查失败。
- 自动定点搜索会排除活帧计数器无法到达的目标帧，避免因 NPC 步长造成的不可达候选误触发或反复重测种。

# 3.0.0

- 重做视频源连接与预览，采集卡画面可同时用于主预览、独立预览、Seed/TID 捕捉、OCR 和伊机控；新增 OBS 投影窗口捕捉，并改进视频源及眼睛区域配置错误提示
- Windows 绿色包内置原生伊机控运行环境，无需另行安装 EasyCon、EasyConBridge 或 `ezcon`；页面脚本、自动流程和虚拟手柄共用同一串口连接
- 脚本统一放在 exe 同级的 `script` 目录，旧选择和用户自定义脚本会保留；新增神奥图鉴和优化版 BDSP 过帧脚本，并补充、优化多份定点和游走脚本
- 新增应用内增量更新，默认在启动时静默检查且可在帮助菜单关闭；更新会保留用户脚本和配置，失败时自动恢复旧版
- 自动定点新增可选的“逃跑续搜”，并优化候选结果、反查历史、脚本选择及主动停止流程
- 优化 OCR 默认区域、判闪识别和任务管理，减少首次识别等待与并发误判，并兼容繁体版本
- 选择草苗龟、小火焰猴或波加曼时，判闪计时会改用“去吧/上吧”到战斗按钮出现的时间差；其他目标的判闪方式保持不变
- 新增默认开启的七天运行日志，每天汇总为一个文件，可在“帮助 -> 运行日志”中关闭、查看或打开日志目录
- 优化小屏和高 DPI 下的界面布局、独立预览框选、参数说明、更新日志及用户可见的状态和错误提示

# 2.1.7

- 优化 OCR 内置模型预热：安装包强制校验并使用随包模型，预热期间禁用同步识别与测试，减少首次识别等待和并发异常

# 2.1.6

- 自动定点和自动 TID 在后续轮次运行测种脚本前检测“缩放/縮放”“锁定/鎖定”“重新调整/重新調整”等放大提示，必要时通过双 HOME 自动退出放大模式，并完善停止竞态保护
- 启动时预热共享 OCR 资源，完善预热线程的关闭与复用生命周期，减少流程首次 OCR 的等待
- 20、40、64 次眨眼捕捉每累计 10 次会通过伊机控短按 L 肩键保持 Switch 亮屏，覆盖手动 Seed/TID、自动定点和自动 TID 捕捉
- 完善亮屏保活的 CLI/Bridge 非阻塞、超时和故障恢复；伊机控异常不会冻结界面或中断捕捉，失效 Bridge 可直接重新连接

# 2.1.5

- 普通 reidentify 与 `Reidentify 1 PK NPC` noisy reidentify 优先使用 C++ 原生搜索；结果与 Project_Xs 原版一致，原生不可用、输入不支持或无匹配时继续回退到 Python 实现
- 自动流程按预期 advances 缩小重新识别范围并记录搜索范围、计算后端和耗时；手动 noisy reidentify 保留当前活帧快照，避免停止活帧后从 0 帧重新搜索
- 原生重新识别显著减少 Python 列表、切片和候选分配，普通 100 万帧与 noisy 10 万帧搜索在本机基准中均获得约百倍加速
- 修正 Python 回退与 C++ 原生定点生成器的 RNGList 推进及强制闪 PID 计算，使两条路径继续与 PokeFinder 样例一致

# 2.1.4

- 修复关于项目页微信/支付宝赞赏按钮在二维码缺失或读取失败时看起来无响应的问题
- 自动定点“预留帧数”默认值调整为 500000，已有历史设置时继续恢复用户记录

# 2.1.3

- 修复自动定点目标添加未同步存档版本的问题，明亮珍珠可正确选择急冻鸟、闪电鸟、火焰鸟等版本限定目标
- 补全三鸟反查组，选择急冻鸟、闪电鸟、火焰鸟任一目标时会按三鸟整组反查
- 自动定点闪光阈值默认调整为 4.0 秒，已有历史设置时继续恢复用户记录
- 修复错误眼睛区域可能稳定算出同一假 seed 后反复无候选、无限循环的问题
- 修复主窗口存档信息不会记忆的问题，重开软件后会恢复存档名称、TID、SID、版本和护符/图鉴勾选状态

# 2.1.2

- 优化自动TID目标池布局

# 2.1.1

- 优化自动乱数中的输出

# 2.1.0

- 修复ocr识别bug：原本只识别`去吧`作为判断，现在额外增加`上吧`作为判断

# 2.0.2

- 增加ocr判闪机制：如果识别到 `出现了!`会多等30s `去吧`才会判定为超时
- 增加`出现了!`和`去吧`的时间输出

# 2.0.1

- 增加单片机连接失败提示
- 修复主动停止却有失败弹窗

# 2.0.0

- 反查失败输出能力值
- 修复过帧脚本的切换精灵失败问题
- 增加自动TID功能
- 增加过场脚本和预留帧数（为在精灵面前测帧的需求增加）
- 增加ocr预热以及ocr范围框选
- 修复手柄按键映射无法修改问题
- 增加自行校验闪光按钮

# 1.0.1

- 增加框选眼睛时的确认框
- 修复帧推进异常问题
- 优化撞闪脚本的触发方式

# 1.0.0

- 支持对定点宝可梦进行自动化乱数，目前阿尔宙斯和谢米内置脚本
- 支持自动反查功能（检查具体delay）
- 支持同步功能（队首同步可更改宝可梦体型从而高效寻找目标）
- 支持伊机控CLI模式和常驻连接模式（常驻连接尚有bug）
- 支持多目标筛选
- 增加三狗、三柱、水都的多反查机制
