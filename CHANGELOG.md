# 2.2.0

- 新增共享采集卡视频源：独立 Capture Broker 以 `1920x1080 / MJPG / 30fps` 独占采集卡，常驻预览、画中画、Project_Xs 眨眼识别、OCR 和伊机控搜图可同时读取最新完整帧且互不阻塞；允许消费者丢帧，首帧 5 秒超时，运行中 1 秒无新帧即明确失败
- 伊机控产品主路径改为项目内维护的 Python 原生实现，不再要求 EasyCon、EasyConBridge、`ezcon.exe` 或 `EASYCON_ROOT`；支持长期串口连接、现有 `.txt` / `.ecs` 脚本、`IMPORT/lib`、`.IL` 搜图和 `TesserDetect`，搜图范围框与命中框默认显示在预览副本并可独立关闭
- 新增应用内增量升级：正式 Windows 包可在“帮助 -> 检查更新…”中发现新版，只下载相对当前版本发生变化的文件；独立升级器会复验官方 SHA-256、串行化同一安装目录的升级，以持久事务替换文件，并在新版确认启动前保留旧文件，安装中断或启动失败会恢复旧版。应用启动不会自动联网，只有用户主动检查更新时才访问 GitHub Releases。用户修改过的脚本、运行日志、Project_Xs 配置和自定义眼图不会被强制覆盖，关闭前也会提示保存未提交的伊机控脚本。首次安装带升级器的版本仍需下载一次完整包，缺少连续增量升级链时会回退到完整 Release 页面。
- 自动定点新增可选的“逃跑续搜”：OCR 明确判定未出闪且搜索范围内仍有后续候选时，运行独立逃跑脚本，随后校正当前位置并按原自动流程继续最近的可达目标；该流程可连续重复，遇到需要过场的目标时仍按“过帧、过场脚本、过场校正、撞闪”执行，过场后不会重新测 Seed，运行日志和历史记录会标明“第 N 轮 / 第 M 次”。OCR 无法确认是否出闪时会停止并等待人工处理，没有后续候选时保留原有自动反查和循环收尾逻辑
- 优化自动定点脚本选择区：六个脚本下拉框每次展开时自动重扫 `script` 目录并保留仍然有效的选择，移除手动“刷新脚本列表”按钮；新增逃跑脚本选择及开关记忆，并修正“逃跑续搜”文字、勾选框的对齐和背景显示
- 新增 Windows OBS 投影窗口捕捉兼容：匹配到 OBS 进程时自动使用 Qt/Windows Graphics Capture，可用于实时预览、Seed/TID 捕捉、校正及宝可梦眨眼捕捉；自动裁剪标题栏和边框，改善窗口缩放、高 DPI 显示、低分辨率提示及捕捉资源释放。OBS 投影无需保持置顶，但必须保持打开且不能最小化；摄像头、PotPlayer 和其他普通窗口继续使用原有捕捉机制
- 新增可选的七天运行日志：可在“帮助 -> 运行日志”中开启自动保存或打开日志目录，记录应用、自动定点、自动 TID、Seed 捕捉、OCR、伊机控和异常等关键事件；日志默认关闭，按日期和会话保存，自动清理超过七个自然日的文件，并在写入前隐藏 Windows 用户目录信息
- OCR 区域首次使用或尚未配置时自动载入内置的性格、个性和六项能力值默认区域，已有用户设置及主动清空的区域不会被默认值覆盖
- 自动策略的“搜索范围”“delay”“最大等待窗口”“预留帧数”和“闪光阈值（秒）”新增完整中文悬停说明，标题和数值框均可触发；原“最大帧数”更名为“搜索范围”，并注明全国图鉴齐全时过 100 万帧大约需要 10 分钟
- 面向用户的界面、阶段状态、错误提示和日志统一将 `reidentify` 显示为“校正”，过场相关结果显示为“过场校正”；内部 CLI 子命令、配置键和兼容接口保持不变
- 修复主动停止自动流程时，CLI 模式下正在运行的 EasyCon 脚本仍可能继续执行的问题；现在会终止对应 ezcon 子进程并按“已取消”状态收敛，同时保留正常运行时的输出和错误分类

# 2.1.7

- 优化 OCR 内置模型预热：安装包强制校验并使用随包模型，预热期间禁用同步识别与测试，减少首次识别等待和并发异常

# 2.1.6

- 自动定点和自动 TID 在后续轮次运行测种脚本前检测“缩放/縮放”“锁定/鎖定”“重新调整/重新調整”等放大提示，必要时通过双 HOME 自动退出放大模式，并完善停止竞态保护
- 启动时预热共享 OCR 资源，完善预热线程的关闭与复用生命周期，减少流程首次 OCR 的等待
- 20、40、64 次眨眼捕捉每累计 10 次会通过伊机控短按 L 肩键保持 Switch 亮屏，覆盖手动 Seed/TID、自动定点和自动 TID 捕捉
- 完善亮屏保活的 CLI/Bridge 非阻塞、超时和故障恢复；伊机控异常不会冻结界面或中断捕捉，失效 Bridge 可直接重新连接

# 2.1.5

- 普通 reidentify 与 `Reidentify 1 PK NPC` noisy reidentify 优先使用 C++ 原生搜索；结果与 Project_Xs 原版一致，原生不可用、输入不支持或无匹配时继续回退到 Python 实现
- 自动流程按预期 advances 缩小重新识别范围并记录搜索范围、计算后端和耗时；手动 noisy reidentify 保留当前活帧快照，避免停止活帧后从 0 帧重新搜索
- 原生重新识别显著减少 Python 列表、切片和候选分配，普通 100 万帧与 noisy 10 万帧搜索在本机基准中均获得约百倍加速
- 修正 Python 回退与 C++ 原生定点生成器的 RNGList 推进及强制闪 PID 计算，使两条路径继续与 PokeFinder 样例一致

# 2.1.4

- 修复关于项目页微信/支付宝赞赏按钮在二维码缺失或读取失败时看起来无响应的问题
- 自动定点“预留帧数”默认值调整为 500000，已有历史设置时继续恢复用户记录

# 2.1.3

- 修复自动定点目标添加未同步存档版本的问题，明亮珍珠可正确选择急冻鸟、闪电鸟、火焰鸟等版本限定目标
- 补全三鸟反查组，选择急冻鸟、闪电鸟、火焰鸟任一目标时会按三鸟整组反查
- 自动定点闪光阈值默认调整为 4.0 秒，已有历史设置时继续恢复用户记录
- 修复错误眼睛区域可能稳定算出同一假 seed 后反复无候选、无限循环的问题
- 修复主窗口存档信息不会记忆的问题，重开软件后会恢复存档名称、TID、SID、版本和护符/图鉴勾选状态

# 2.1.2

- 优化自动TID目标池布局

# 2.1.1

- 优化自动乱数中的输出

# 2.1.0

- 修复ocr识别bug：原本只识别`去吧`作为判断，现在额外增加`上吧`作为判断

# 2.0.2

- 增加ocr判闪机制：如果识别到 `出现了!`会多等30s `去吧`才会判定为超时
- 增加`出现了!`和`去吧`的时间输出

# 2.0.1

- 增加单片机连接失败提示
- 修复主动停止却有失败弹窗

# 2.0.0

- 反查失败输出能力值
- 修复过帧脚本的切换精灵失败问题
- 增加自动TID功能
- 增加过场脚本和预留帧数（为在精灵面前测帧的需求增加）
- 增加ocr预热以及ocr范围框选
- 修复手柄按键映射无法修改问题
- 增加自行校验闪光按钮

# 1.0.1

- 增加框选眼睛时的确认框
- 修复帧推进异常问题
- 优化撞闪脚本的触发方式

# 1.0.0

- 支持对定点宝可梦进行自动化乱数，目前阿尔宙斯和谢米内置脚本
- 支持自动反查功能（检查具体delay）
- 支持同步功能（队首同步可更改宝可梦体型从而高效寻找目标）
- 支持伊机控CLI模式和常驻连接模式（常驻连接尚有bug）
- 支持多目标筛选
- 增加三狗、三柱、水都的多反查机制
